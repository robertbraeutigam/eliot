/**
 * The Boolean truth values, `true` and `false`. Branch on a `Bool` with `fold`.
 */
type Bool

/**
 * Logical conjunction: `true` when both `a` and `b` are `true`.
 */
infix def &&(a: Bool, b: Bool): Bool
