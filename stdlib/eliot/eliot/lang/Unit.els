import eliot.compiler.Meta

type Unit

def unit: Unit

/**
 * The trivial refinement domain: `Unit` is the meta of every type that carries no refinement.
 */
implement Meta[Unit] {
   def join(a: Unit, b: Unit): Unit = a
}
