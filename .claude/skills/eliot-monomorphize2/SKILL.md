---
name: eliot-monomorphize2
description: Use when editing, debugging, or reasoning about code under `lang/src/com/vanillasource/eliot/eliotc/monomorphize2/` — the constraint-based type checker for Eliot. Covers the responsibility boundary between the structural `OperatorResolvedExpression` (ORE) representation and the evaluated `Value` / `ExpressionValue` representation, the constraint extraction → solve → substitute pipeline, and the hard rule that evaluation (even partial) must only happen in the `eval` package.
---

# monomorphize2: constraint-based type checker

## Scope of this skill

This skill governs all code under:

```
lang/src/com/vanillasource/eliot/eliotc/monomorphize2/
├── fact/                 (MonomorphicValue, MonomorphicExpression)
├── processor/            (MonomorphicTypeCheckProcessor)
└── typecheck/
    ├── constraints/      (ConstraintExtract, Constraints, TypeCheckState, ShortUniqueIdentifiers)
    └── solution/         (ConstraintSolver, Solution, SolverState)
```

and its sibling tests under `lang/test/src/com/vanillasource/eliot/eliotc/monomorphize2/`.

`monomorphize2` is a **new, parallel** implementation of the monomorphic type checker. The old `monomorphize` package is still the one wired into `LangPlugin.initialize` at `lang/src/com/vanillasource/eliot/eliotc/plugin/LangPlugin.scala:80`. Do **not** touch the old `monomorphize` package when the task is about `monomorphize2`, and do not try to "unify" them.

## The two worlds — and the rule that separates them

There are two representations of types and expressions that flow through `monomorphize2`. They look superficially similar and you must never conflate them:

### World 1: `OperatorResolvedExpression` (ORE) — *structural / syntactic*

Defined at `lang/src/com/vanillasource/eliot/eliotc/operator/fact/OperatorResolvedExpression.scala:10`.

Cases: `FunctionApplication`, `IntegerLiteral`, `StringLiteral`, `ParameterReference`, `ValueReference`, `FunctionLiteral`.

- ORE is **pure syntax** — an AST after name resolution and operator resolution. It carries no evaluated information.
- `substitute(expr, name, replacement)` and `containsVar(expr, name)` are the only manipulations you may perform on ORE inside `monomorphize2`. They are purely syntactic.
- Inside `monomorphize2`, **both sides of a constraint are ORE**. Type checking is unification of ORE terms, not of evaluated values.
- A `ParameterReference` in ORE, inside the type checker, plays a **dual role**: either a real user-written parameter, or a **unification variable** generated by `generateUnificationVar` (its name is a short `$`-suffixed identifier from `ShortUniqueIdentifiers`).

### World 2: `Value` / `ExpressionValue` — *evaluated / semantic*

Defined at `lang/src/com/vanillasource/eliot/eliotc/eval/fact/Value.scala` and `lang/src/com/vanillasource/eliot/eliotc/eval/fact/ExpressionValue.scala`.

- `Value` is the fully-ground, fully-evaluated form: `Direct`, `Structure`, `Type`. Every `MonomorphicExpression.expressionType` is a `Value`.
- `ExpressionValue` is the partially-evaluated form used *inside* the evaluator: `ConcreteValue`, `FunctionLiteral`, `NativeFunction`, `ParameterReference`, `FunctionApplication`. `concreteValueOf` projects it down to a `Value` when it is ground.
- All evaluation, reduction, beta-reduction, substitution-inside-values, lambda-stripping, and type-argument application lives in the `eval` package and is only accessed through `eval.util.Evaluator` and the `ExpressionValue` companion object.

### The hard rules (read before changing anything)

These are invariants of the `monomorphize2` design. Breaking any of them is a bug even if tests pass.

1. **ORE is structure, not a value.** Do not try to evaluate an `OperatorResolvedExpression` inside `monomorphize2` by hand. Do not write recursive functions inside `monomorphize2` that reduce ORE to a "normal form", beta-reduce lambdas, or fold constants. If you need an evaluated answer, call `Evaluator.evaluate(...)` from the `eval` package.
2. **Do not assume any specific ORE structure.** In particular:
   - Do **not** assume that a type expression starts with leading `FunctionLiteral`s.
   - Do **not** assume that the number of leading lambdas corresponds to the number of type parameters.
   - Do **not** unwrap lambdas yourself to "get at the body".
   - Do **not** try to add a new ORE node, just because it would be convenient in `monomorphize2`
3. **Evaluation — and partial evaluation — happens in the `eval` package only.** `monomorphize2` is allowed to *call* `Evaluator.evaluate`, `Evaluator.applyTypeArgs`, and the `ExpressionValue` helpers. It is not allowed to *reimplement* any part of them. If you find yourself writing something that looks like a mini-evaluator, stop and move it to `eval`.
4. **No duplication of responsibility.** Every piece of logic has exactly one home:
   - Parse → `token`, `ast`, `core`
   - Name resolution → `resolve`
   - Match desugaring → `matchdesugar`
   - Operator resolution → `operator`
   - Ability/typeclass resolution → `implementation`
   - **Evaluation / reduction → `eval`**
   - **Constraint generation and unification → `monomorphize2`**
   - Uncurrying → `uncurry`
   If a piece of code in `monomorphize2` is doing the job of another phase, it is in the wrong place.
5. **Constraints are ORE ≡ ORE.** A `Constraints.Constraint` at `lang/src/com/vanillasource/eliot/eliotc/monomorphize2/typecheck/constraints/Constraints.scala:15` has a `left: OperatorResolvedExpression` and a `right: Sourced[OperatorResolvedExpression]`. Do not change these to `Value` or `ExpressionValue`; the whole point is that the solver works structurally and only calls out to the evaluator as a fallback.

## Pipeline at a glance

`MonomorphicTypeCheckProcessor.generateFromKeyAndFact` at `lang/src/com/vanillasource/eliot/eliotc/monomorphize2/processor/MonomorphicTypeCheckProcessor.scala:30` is the entry point. It runs three phases in order:

### Phase 1 — Constraint extraction (`ConstraintExtract.collectConstraints`)

File: `lang/src/com/vanillasource/eliot/eliotc/monomorphize2/typecheck/constraints/ConstraintExtract.scala`.

- Walks the `TypeStack` of the resolved value from top (kind `Type`) down to the signature, and then the runtime body against the signature.
- At each ORE node, it emits `Constraint(assumedType, expression.as(actualType), "…")` where *both* sides are ORE.
- **Records the assumed type at each visited node into `TypeCheckState.nodeAssumedTypes`** (keyed by the node's `Sourced[OperatorResolvedExpression]` wrapper). This side-table is what Phase 3 reads to discover each node's expected type — there is no replay of fresh-variable generation in the processor walk. The recording happens automatically at the entry of `collectConstraints`; new ORE cases get it for free as long as they recurse through `collectConstraints`.
- Allocates fresh unification variables via `generateUnificationVar` — these are just fresh short names used inside `ParameterReference` nodes.
- Runs in `TypeGraphIO = StateT[CompilerIO, TypeCheckState, _]`. State: `parameterTypes`, `nodeAssumedTypes`, `constraints`, `shortIds`. Use the helpers in `TypeCheckState` (`bindParameter`, `lookupParameter`, `recordAssumedType`, `tellConstraint`, `generateUnificationVar`) — do not mutate the state object directly.
- When adding a new ORE case: emit constraints; do not evaluate; use `expression.as(...)` to propagate source positions.

**Key invariants for the FunctionLiteral case** (these were learned the hard way):

- **`bindParameter` stores the declared annotation expression itself**, not a fresh unification variable. For `(A: Type) -> ...`, bind `A → Type`. For `(a: A) -> ...`, bind `a → ParameterReference("A")`. If you bind to a fresh "identity" variable (the old `paramVar` pattern), the body's references propagate that variable into kind-checking slots and unification will pin it to the slot's expected kind — conflating the parameter's identity with its kind. Without an annotation, a fresh unification var is fine because there is no real declared type to preserve.
- **The `ParameterReference` case returns `expression.value`** (the original parameter reference), not the lookup result. The constraint *side effect* still uses the lookup (so kind constraints are emitted), but the *return value* preserves the original structure so it can flow up the recursive return chain into the body of the enclosing `FunctionLiteral`.
- **The `FunctionLiteral` case returns `expression.value`** (the original `FunctionLiteral`), not a synthesized `funcType` like `Function(paramType)(retType)`. The lambda structure has to reach the solver intact, otherwise the solver cannot beta-reduce a redex it later encounters as part of the runtime check. The constraints are still emitted correctly inside the case; only the *return* preserves structure.
- The recursive return chain from extraction is therefore essentially an identity transform on the ORE — every case returns `expression.value`. The interesting work is the constraints generated as side effects in `TypeGraphIO`.

**Specified type arguments** (`key.specifiedTypeArguments`) must be applied **to the signature returned by the typeStack fold**, not by replacing the bottom element of `typeExpressions` before the fold runs. Replacing the bottom element breaks the kind relationship of the level above: the kind of `((A: Type) -> T(A))(BigInteger)` is `Type`, not the original kind of the unsubstituted signature. Apply them like this:

```scala
monomorphizedSignature = key.specifiedTypeArguments
                           .foldLeft(resolvedValue.typeStack.as(signature)) { (acc, arg) =>
                             acc.as(FunctionApplication(acc, arg))
                           }
```

Then check the runtime body against `monomorphizedSignature.value`. The solver beta-reduces the resulting `FA(FL, typeArg)` redex when unifying against the runtime body's type.

**Explicit type arguments at value reference call sites** (`vr.typeArgs`) must be applied the same way inside the `ValueReference` case — fold them over the resolved value's signature with `FunctionApplication` wrappers before emitting the constraint:

```scala
case ValueReference(vfqn, typeArgs) =>
  for {
    resolvedMaybe <- StateT.liftF(getFact(OperatorResolvedValue.Key(vfqn.value)))
    _             <- resolvedMaybe match {
                       case Some(resolved) =>
                         val appliedSig =
                           typeArgs.foldLeft(resolved.typeStack.map(_.signature)) { (acc, arg) =>
                             acc.as(FunctionApplication(acc, arg))
                           }
                         tellConstraint(
                           Constraints.constraint(assumedType, appliedSig, "Type mismatch.")
                         )
                       case None => StateT.liftF(compilerAbort(vfqn.as("Value not defined.")))
                     }
  } yield expression.value
```

If you skip the fold and emit `assumedType := resolved.typeStack.map(_.signature)` directly, then call sites like `Box[two]` and `Box[one]` look identical to the solver and explicit type-argument mismatches go undetected. The solver's beta-reduction rules consume the wrapping the same way they consume the top-level `key.specifiedTypeArguments` wrapping.

### Phase 2 — Solving (`ConstraintSolver.solve`)

File: `lang/src/com/vanillasource/eliot/eliotc/monomorphize2/typecheck/solution/ConstraintSolver.scala`.

`solve` takes both the constraints and the post-extraction `endState.shortIds`, seeded into `SolverState` so the solver can generate fresh unification variables that don't collide with the ones the extractor introduced. `SolverState` therefore carries `pending`, `bindings`, **and** `shortIds`; use `generateUnificationVar: SolverIO[String]` to allocate.

Iterative worklist over ORE constraints. `tryResolve` handles, in order:

1. **Trivial**: `ParameterReference(x) ≡ ParameterReference(x)` — done.
2. **Beta-reduction (left)**: left side is `FunctionApplication(FunctionLiteral(p, _, body), arg)` — substitute `p := arg.value` in `body` and re-enqueue `body[p := arg] := right`.
3. **Beta-reduction (right)**: right side is `FunctionApplication(FunctionLiteral(p, _, body), arg)` — substitute `p := arg.value` in `body` and re-enqueue `left := body[p := arg]`.
4. **Polytype instantiation (left)**: left side is a top-level `FunctionLiteral(p, _, body)` (an *unapplied* polytype, e.g. the unmonomorphized signature of a referenced generic value). Generate a fresh unification variable `?V`, substitute `p := ParameterReference(?V)` in `body` purely syntactically (`OperatorResolvedExpression.substitute`), and re-enqueue `body[p := ?V] := right`. This is HM-style polytype instantiation: the solver specializes the polytype to a fresh monotype before unification.
5. **Polytype instantiation (right)**: same as above, mirrored.
6. **Structural decomposition** on matching shapes: both `FunctionApplication`, or both `ValueReference` with the same name and arity — enqueue children as sub-constraints.
7. **Variable binding**: one side is a `ParameterReference` — bind it, with occurs-check via `OperatorResolvedExpression.containsVar` (not `ExpressionValue.containsVar`).
8. **Fallback — `evalAndCompare`**: only when none of the above apply, reduce both sides via `Evaluator.evaluate` and compare the resulting `ExpressionValue`s as concrete values. If neither is concrete yet, `defer`.

**The order matters in two distinct ways:**

- **Beta-reduction must precede structural FA decomposition.** If `FA(FL, arg) := FA(g, y)` were structurally decomposed first, the solver would try to unify `FL := g` and `arg := y` independently — but a beta-redex must reduce as a whole. The lambda body has to be substituted with its actual argument before the result can be compared to anything else.
- **Beta-reduction must precede polytype instantiation, and instantiation must precede variable binding.** A real beta-redex `FA(FL, arg)` must reduce with the user's actual argument, not with a fresh unification variable. And `?V := polytype` must instantiate the polytype *before* binding `?V`, otherwise `?V` ends up bound to the un-instantiated polytype itself and downstream uses re-encounter the same problem.

The new instantiation cases are what allow constraints like `Function(F$)(G$) := (A: Type) -> Function(A)(A)` (which appear whenever a generic value reference like `id` is used at a call site without explicit type arguments) to make progress. Without them, the constraint is structurally stuck — neither side is decomposable, neither is a `ParameterReference`, the fallback reaches a non-concrete `ExpressionValue` and defers forever, and the test reports `Could not resolve type`.

The worklist loop (`propagate`) keeps retrying until either everything resolves, nothing new resolves (→ report unresolved), or a compile error is issued.

Invariants to preserve when editing the solver:

- Bindings are stored fully substituted. `SolverState.bind` rewrites existing bindings' RHS to preserve this. Do not introduce a new path that writes to `bindings` without going through `bind`.
- Never add a branch that evaluates ORE by hand — extend `evalAndCompare` or add a new structural case, do not "peek through" lambdas. The beta-reduction and instantiation cases above are the only allowed exceptions, and they only call `OperatorResolvedExpression.substitute`, which is purely syntactic.
- `issueOreError` vs `issueValueError`: the former for structural-shape mismatches (before evaluation), the latter only for concrete-value mismatches discovered in the fallback.
- The instantiation cases must use `generateUnificationVar` from `SolverState`, not invent ad-hoc names. Names must be guaranteed unique against the extractor's existing names — that's why `SolverState` is seeded with `endState.shortIds`.

### Phase 3 — Type substitution and MonomorphicExpression building

`typeSubstitute`, `buildExpression`, and `nodeType` in `MonomorphicTypeCheckProcessor.scala`.

- `typeSubstitute` evaluates the signature (via `Evaluator.evaluate` + `Evaluator.applyTypeArgs`), resolves parameter types using the `Solution`, and then calls `buildExpression` to produce a `MonomorphicExpression` for the runtime body.
- `buildExpression` is a plain `CompilerIO` recursive walk over the runtime ORE. There is no monad transformer for fresh-variable replay and no top-down `assumedType` parameter — every node is independent.
- `buildExpression` converts an ORE body into a `MonomorphicExpression` (`fact/MonomorphicExpression.scala`). Every `MonomorphicExpression` carries a `Value` as its `expressionType` — this is the point where you leave "ORE world" and enter "Value world".

**Each node's type is read from the side-table.** The constraint extractor recorded, for every visited ORE node, the assumed (expected-from-parent) ORE type at that node into `endState.nodeAssumedTypes`, keyed by the same `Sourced[OperatorResolvedExpression]` wrapper that flows through both the extractor and the processor. The processor's `nodeType` helper reads that ORE, evaluates it (`Evaluator.evaluate`), substitutes the solver's bindings into the resulting `ExpressionValue` (`Solution.resolveExpressionValue`), runs `Evaluator.reduce` once more to apply native type constructors that became reducible only after substitution, and finally projects to a concrete `Value` via `concreteValueOf`.

Why the second `reduce`: `evaluate` produces a partially reduced `ExpressionValue` whose unification-variable `ParameterReference`s have not yet been resolved. After `resolveExpressionValue` substitutes them with `ConcreteValue` wrappers, expressions like `FA(NativeFunction, ?A)` become `FA(NativeFunction, ConcreteValue(BigInt))`, which the native side can now consume. Skipping the reduce step leaves a `FunctionApplication` chain that `concreteValueOf` cannot project, and you get spurious "Type at this position did not resolve to a concrete value" errors at every ValueReference whose type involves a native type constructor like `Function`.

At each node, `buildExpression` looks like:

- **`ValueReference(vfqn, _)`**: `nodeType(source)` and emit `MonomorphicExpression(ty, MonomorphicValueReference(vfqn, Seq.empty))`. Do **not** evaluate the resolved value's signature, do **not** call `applyTypeArgs(_, Seq.empty, _)`, do **not** strip leading lambdas. The recorded assumed-type ORE for this position already encodes the inferred concrete type after solving.
- **`FunctionApplication(target, arg)`**: `nodeType(source)`, then recurse into `target` and `arg`. Each child reads its own assumed type from the side-table independently — there is no need to compute `targetType` / `argType` in the parent.
- **`FunctionLiteral(paramName, _, body)`**: `nodeType(source)`, then recurse into `body`. Take `paramType` from `paramTypes` (which `resolveParameterTypes` precomputes by evaluating each `endState.parameterTypes` entry through `Solution.resolveExpressionValue`).
- **`ParameterReference(name)`**: take the type from `paramTypes`, falling back to `nodeType(source)` if the name is missing.
- **Literals**: return `bigIntType` / `stringType` directly. The side-table also has an entry for them, but they have a known constant type so the lookup is unnecessary.

**Invariants for any change to the processor walk:**

- Every recursive call into `buildExpression` must use the original `Sourced` wrapper of the child sub-expression (not a synthesized wrapper), so the side-table lookup matches what the extractor recorded.
- Do not pass an `assumedType` parameter top-down — every node's type comes from the side-table. Do not derive a node's type by recursing into the ORE bottom-up either.
- When adding a new ORE case, you only need to handle it in `buildExpression`. The extractor's `recordAssumedType` already covers the side-table side as long as the new case calls `collectConstraints` on its sub-expressions.
- Always call `Evaluator.reduce` after `Solution.resolveExpressionValue` in `nodeType`. Skipping reduce silently breaks any node whose type involves a native type constructor.

## Diagnosing a type-checker failure

When a test or example produces a wrong type or a bogus "Could not resolve type" error:

1. Run the test with debug logging and grep for the trace. `Constraints.debugConstraints` prints every emitted constraint before solving, and the solver prints `Checking L vs. R` for every iteration.
2. If the **extracted constraints are wrong**, the bug is in `ConstraintExtract`. Walk the offending ORE case by hand against the code.
3. If the constraints are right but the solver **fails to bind / decompose**, the bug is in `ConstraintSolver.tryResolve` — probably a missing structural case, a beta-redex that isn't being reduced, or a top-level `FunctionLiteral` polytype that isn't being instantiated.
4. If the solver reaches `evalAndCompare` and it returns a wrong answer, the bug is probably in the `eval` package (not here). Check `Evaluator.evaluate` against the ORE being passed in. Do not "fix" it by adding compensation logic in the solver.
5. If the solver binds a unification var to an expression that still contains free unification vars, make sure `SolverState.bind` is being used (not a direct map update) so that existing bindings get re-substituted.

**Conflation diagnostic — read the Solution map.** If a unification variable that conceptually represents a parameter's *identity* (e.g., a fresh var introduced when binding a type parameter) ends up resolved to a kind expression like `Type`, the constraint extractor is conflating identity with kind. The chain to look for: the var appears in a slot (e.g., a `Function(_)(_)` argument position) that is independently constrained to a specific kind, and unification pins the var to that kind. Fix: don't bind parameters to fresh identity vars in the first place — bind them to their declared annotation expression.

**Stuck FA(FL, _) on one side of an unresolved constraint.** If the unresolved constraint shows a `FunctionApplication` with a `FunctionLiteral` head against an unrelated structure (most often after the runtime check), the solver is missing the beta-reduction case for that side, or the beta-reduction case isn't firing because the cases above it consumed the constraint first. The beta-reduction cases must come *before* structural FA decomposition.

**Stuck `Function(?_)(?_) := (A: Type) -> …` (an unapplied polytype on one side).** This means a generic value reference was used and the polytype never got instantiated. The solver's polytype instantiation rules (cases 4 and 5 of `tryResolve`) must be present and must fire — check that they sit between beta-reduction and structural FA decomposition, and that `SolverState` is seeded with `endState.shortIds` so `generateUnificationVar` produces names that don't collide with the extractor's.

**`Could not resolve type` paired with a stuck polytype constraint** is the same diagnosis: missing or misordered instantiation rule. Check the debug trace for `Checking Function(_)(_) vs. (Type :: A) -> …` lines that never make progress.

**`Non-generic type signature did not evaluate to concrete value` pointing inside a body.** This is the symptom of a `buildExpression.ValueReference` case calling `Evaluator.applyTypeArgs(stripLeadingLambdas(rawValueType), Seq.empty, vfqn)` on a polytype value reference. In the current design, `buildExpression.ValueReference` must just compute its type via `nodeType(source)` — which reads the recorded assumed type ORE, evaluates and resolves it. If you see this error, somebody re-introduced a per-value signature evaluation in the processor walk. Delete it.

**`Type at this position did not resolve to a concrete value` at a `ValueReference` whose type involves `Function` (or another native type constructor).** `nodeType` skipped the post-resolve `Evaluator.reduce` step. After `Solution.resolveExpressionValue` substitutes unification vars with `ConcreteValue`s, expressions like `FA(NativeFunction, ?A)` become `FA(NativeFunction, ConcreteValue(BigInt))`, which `concreteValueOf` cannot project until the native side actually consumes the argument. Always call `Evaluator.reduce(resolved, source)` between `resolveExpressionValue` and `concreteValueOf`.

**`Internal error: no assumed type recorded for this node`.** The processor walked a `Sourced[OperatorResolvedExpression]` that does not appear in `endState.nodeAssumedTypes`. Either the extractor never visited this node (missing recursive `collectConstraints` call in a new ORE case), or the processor walked a *synthesized* sub-expression with a fresh wrapper instead of the original `Sourced` one the extractor recorded. The processor must descend through the same `Sourced` wrappers the extractor did so case-class equality matches.

**Explicit `vr.typeArgs` mismatches go undetected** (e.g., `Box[two]` where `Box[one]` is expected, but no error is reported). The `ConstraintExtract.ValueReference` case is silently dropping `vr.typeArgs` instead of folding them as `FunctionApplication` wrappers around the resolved signature. See the `ValueReference` snippet in Phase 1 above.

## Testing

Tests live at:

- `lang/test/src/com/vanillasource/eliot/eliotc/monomorphize2/processor/MonomorphicTypeCheckProcessorTest.scala` — end-to-end tests that run the whole pipeline from source text to `MonomorphicValue`. Use `runEngineForMonomorphicValue(source, name, typeArgs)`.
- `lang/test/src/com/vanillasource/eliot/eliotc/monomorphize2/processor/MonomorphicTypeCheckTest.scala` — the larger test suite.

Run them:

```bash
./mill lang.test 2>&1 | grep -v DEBUG | grep "MonomorphicTypeCheck"
```

Follow the project testing conventions: single-line asserts where possible, assert the `Seq` itself (don't split length and items), prefer `.asserting(_ ...)` form. Do not introduce a new `.els` fixture file for a type checker test — the existing tests construct sources inline.

## Anti-patterns (reject in review)

- Pattern-matching on a `FunctionLiteral` at the head of an ORE type expression *inside `ConstraintExtract` or the processor walk* to "strip off" type parameters. (The solver's polytype instantiation cases do pattern-match on a top-level `FunctionLiteral`, but they only peel one level at a time via `OperatorResolvedExpression.substitute` with a fresh unification variable, and they do so as part of unification, not as bulk lambda stripping. That is the *only* place where pattern-matching on a top-level `FunctionLiteral` is allowed.)
- Calling `ExpressionValue.stripLeadingLambdas` or `extractLeadingLambdaParams` on the result of something that is still an ORE. (`stripLeadingLambdas` takes an `ExpressionValue` — if you are tempted to call it on an ORE, you are in the wrong world.)
- A new helper inside `monomorphize2` that takes an ORE and returns a "reduced" ORE. That is an evaluator. Put it in `eval`, or — more likely — do not write it at all because the constraint solver is the reducer here. The solver's beta-reduction and instantiation cases use `OperatorResolvedExpression.substitute` only, which is purely syntactic substitution, not evaluation.
- Adding a `Value` or `ExpressionValue` field to `Constraints.Constraint`. Constraints are ORE ≡ ORE.
- Wiring `monomorphize2.processor.MonomorphicTypeCheckProcessor` into `LangPlugin` without an explicit instruction to do so — the old `monomorphize` processor is still the production path.
- Catching or silently ignoring `compilerAbort` from `Evaluator.evaluate` inside the solver. If evaluation fails, that is a compile error, not a reason to guess.
- **In `ConstraintExtract.FunctionLiteral`**: introducing a fresh `paramVar` (separate from `paramTypeVar`) to bind the parameter name to. This creates an "identity" variable with no constraints connecting it to the actual parameter type, and structural unification will pin it to whatever kind appears at the parameter's first use. Bind to the declared annotation expression directly (or to a fresh var only when there is no annotation).
- **In `ConstraintExtract.ParameterReference` / `FunctionLiteral`**: returning the lookup result, or a synthesized `funcType`, instead of `expression.value`. The recursive return chain has to preserve the original ORE so the solver receives the unevaluated body and can beta-reduce it.
- **In `ConstraintExtract.collectConstraints` (top-level)**: applying `key.specifiedTypeArguments` by replacing the bottom element of `typeExpressions` before the fold. The level above `typeExpressions` then no longer matches the substituted level's actual kind, and you get bogus `Kind1 := retTypeVar` constraints from the outer FA processing. Apply type arguments to the signature *returned* by the fold.
- **In `ConstraintExtract.ValueReference`**: emitting `assumedType := resolved.typeStack.map(_.signature)` directly without folding `vr.typeArgs` over the signature. Without that fold, `Box[one]` and `Box[two]` look identical to the solver and explicit type-argument mismatches go undetected.
- **In `ConstraintSolver.tryResolve`**: placing the structural `FunctionApplication` decomposition case before the beta-reduction cases. A beta-redex on either side must be reduced first; structural decomposition of `FA(FL, x) := FA(g, y)` into `FL := g` and `x := y` is wrong because the redex must reduce as a whole.
- **In `ConstraintSolver.tryResolve`**: placing the polytype instantiation cases before the beta-reduction cases (a real beta-redex would then be specialized with a fresh var instead of reducing with the user's actual argument), or after the variable-binding cases (`?V := polytype` would bind `?V` to the un-instantiated polytype itself, losing the instantiation point and propagating the same problem downstream). The correct order is: trivial → beta-reduction → polytype instantiation → structural decomposition → variable binding → `evalAndCompare`.
- **In `ConstraintSolver`**: inventing fresh-variable names ad-hoc instead of going through `SolverState.generateUnificationVar`, or dropping the `endState.shortIds` seed when constructing `SolverState`. Both lead to name collisions with the extractor's vars.
- **In the processor `buildExpression.ValueReference` case**: calling `Evaluator.applyTypeArgs(stripLeadingLambdas(rawValueType), Seq.empty, vfqn)` (or any variant that tries to compute the value's type from its signature). This crashes on every polytype value reference and produces the misleading `Non-generic type signature did not evaluate to concrete value` error. The case must just compute its type via `nodeType(source)` from the side-table.
- **In the processor walk**: trying to derive a node's type by recursing into the ORE bottom-up (the way the old `buildMonomorphicExpression` did via `targetExpr.expressionType.asFunctionType`). The walk is independent at every node: `nodeType(source)` reads the recorded assumed-type ORE for *this* position, evaluates and resolves it. Bottom-up reconstruction can't handle generic arguments because their concrete types are only knowable from context.
- **In the processor walk**: passing an `assumedType: Value` parameter top-down. The new design has no top-down assumed type — every node reads its own type from `endState.nodeAssumedTypes`. If you find yourself reintroducing this parameter, you are reintroducing the coupling that the side-table was designed to eliminate.
- **In `nodeType`**: skipping the `Evaluator.reduce` step between `Solution.resolveExpressionValue` and `concreteValueOf`. The first `evaluate` call inside `nodeType` cannot reduce native type constructor applications whose arguments are still unification variables; only after substitution do they become reducible. Without the second reduce, every `ValueReference` whose type involves `Function` (or another native type constructor) fails with "Type at this position did not resolve to a concrete value".
- **In the processor walk**: descending into a synthesized `Sourced` wrapper instead of the original child sub-expression. The side-table is keyed by the `Sourced[OperatorResolvedExpression]` wrapper itself (case-class equality), so the processor must walk the same wrappers the extractor recorded. A synthesized wrapper at the same source range will not compare equal because the wrapped value differs.
- **Replaying `ShortUniqueIdentifiers` in the processor**: do not re-introduce a `WalkIO = StateT[CompilerIO, ShortUniqueIdentifiers, _]` monad transformer or an `advanceForExpression` pass over the type-stack levels. The `endState.shortIds` field exists only so the solver can allocate fresh names that don't collide with the extractor's; the processor walk does not need it at all and the side-table makes the replay obsolete.

## Facts and keys (quick reference)

- `MonomorphicValue.Key(vfqn, specifiedTypeArguments)` — composite key. Same `vfqn` + different type args → different specialization → different fact.
- `MonomorphicValue` fields: `vfqn`, `specifiedTypeArguments`, `calculatedTypeArguments` (currently `Seq.empty` — TODO in the processor), `signature: Value`, `runtime: Option[Sourced[MonomorphicExpression.Expression]]`.
- `MonomorphicExpression.expressionType: Value` is always a fully-ground `Value`, never an `ExpressionValue` and never an ORE.
- Downstream consumers of `MonomorphicValue`: `uncurry.processor.MonomorphicUncurryingProcessor` and `used.UsedNamesProcessor`. Keep the shape of `MonomorphicValue` stable when possible; if you must change it, update both consumers.
