import TestSuite

/**
 * A pure test case: it declares only the assertion effect, and the suite finds it by its name alone.
 */
def greeting(name: String): String = "Hello, " ++ name ++ "!"

def test: {Throw[String]} Unit = assertEquals("Hello, Bob!", greeting("Bob"))
