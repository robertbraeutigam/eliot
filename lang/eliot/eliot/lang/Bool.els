/**
 * The Boolean truth values, `true` and `false`.
 *
 * Branch on a `Bool` with `fold`: there is no `if`/`then`/`else` syntax, and `Bool` exposes no
 * pattern-match constructors.
 */
type Bool

/**
 * The true truth value.
 */
def true: Bool

/**
 * The false truth value.
 */
def false: Bool
