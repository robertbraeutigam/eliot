/**
 * Any integer number without restrictions.
 * Reason for being in lang: Integer literals have this type, and the checker/evaluator name it
 * directly (`WellKnownTypes.bigIntFQN`) as the type of type-level integer parameters.
 */
type BigInteger
