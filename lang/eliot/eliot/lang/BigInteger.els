/**
 * An arbitrary-precision integer with no bounds.
 *
 * The type of integer literals and of the `[MIN, MAX]` bounds that parameterise `Int`, so it appears
 * mainly at the type level when describing integer ranges.
 */
type BigInteger
