/**
 * The type of all types — and `Type` is itself a `Type`.
 *
 * Eliot draws no line between the type level and the value level: a type is an ordinary value, and
 * `Type` is the value that classifies them. You rarely write it by hand, since the `[]`
 * type-namespace syntax and generic-parameter kinds cover ordinary use.
 */
type Type
